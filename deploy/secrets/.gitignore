# Ignore every file in this directory by default. Operators drop
# plaintext secrets here (one value per file) and reference them
# from deploy.yaml via `file:./secrets/<name>`.
#
# The directory itself is tracked so the referenced path exists
# in fresh checkouts — only the contents are ignored.
*
!.gitignore
!README.md
